/** Invariant companion plugin (no assertions — nothing to check at runtime). */

/** Provides no assertions: the matrix writes only the preset file — no durable session events. */
export function apply(): void {}
